module.exports = Object.freeze({
  develop: {
    DOMAIN: "localhost",
    PORT: 3001,
    HTTP_PROTOCOL: "http"
  },
  production: {
    DOMAIN: "localhost",
    PORT: 3001,
    HTTP_PROTOCOL: "http"
  }
});
